---
const { class: className, ...rest } = Astro.props
---

<div
  class={`prose prose-lg max-w-full dark:prose-invert prose-h1:text-4xl prose-code:before:!content-none prose-code:after:!content-none sm:prose-h1:text-5xl prose-em:pr-[2px] ${className}`}
  {...rest}
>
  <slot />
</div>

<style is:global>
  .prose code:not(.astro-code code):not(.astro-code-header code) {
    background-color: color-mix(in oklab, rgb(var(--accent)) 15%, transparent);
    border-radius: var(--radius-sm);
    padding-inline: calc(var(--spacing) * 1);
    padding-top: calc(var(--spacing) * 0.5);
  }
</style>
