---
name: Run checks

on:
  push:
  workflow_dispatch:

jobs:
  check:
    uses: ./.github/workflows/setup-and-run-script.yaml
    with:
      script: 'check'
  test:
    uses: ./.github/workflows/setup-and-run-script.yaml
    with:
      script: 'test'
